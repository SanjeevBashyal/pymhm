# -*- coding: utf-8 -*-
"""Tools the plugin offers that are not part of the model-input workflow.

The workflow is what `comboBox_domainDefinitionType`,
`pushButton_executeAllMorphology`, `pushButton_executeMeteoMorphSetup`,
`pushButton_createLatLon`, `pushButton_edit_nmls` and `pushButton_execute_mHM`
drive, end to end. Anything reachable only from its own button lives here --
useful, but nothing in the workflow depends on it.
"""
