# -*- coding: utf-8 -*-
"""The dialog classes themselves, one module per form in `qt/ui/forms`.

A dialog owns its own lifecycle and the widgets the form declares. What each
widget shows lives in `qt/controllers`, the signal wiring in `qt/bindings`.
"""
