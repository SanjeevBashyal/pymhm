# -*- coding: utf-8 -*-
"""Generated Qt Designer user interfaces.

pyuic5 emits a bare ``import resources_rc`` at the foot of any form that uses
the Qt resource system, so the packaged module is registered under that
top-level name here -- before any generated module below can be imported.
"""
import sys

from .... import resources_rc as _resources_rc  # noqa: F401

sys.modules.setdefault("resources_rc", _resources_rc)
